_build/
deps/
.elixir_ls/

contract/node_modules/
contract/test-results/
contract/playwright-report/
contract/blob-report/

priv/static/uploads/*
!priv/static/uploads/.gitkeep

.DS_Store
*.log
erl_crash.dump

# deploy secrets — never commit
.sre-*
*.pem
deploy/.env
__pycache__/

# Apple signing keys must never become git objects.
*.p8

# Host SSH keys. *.pem already covered; these have no extension and were
# committed once into a PUBLIC repo as a result.
.roach-*
